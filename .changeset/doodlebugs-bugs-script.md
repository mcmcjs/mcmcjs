---
"@mcmcjs/doodlebugs": minor
---

Add the JuliaBUGS standalone-script generator (`generateStandaloneScript`), ported from the editor and verified byte-for-byte against its output on the bundled example graphs, alongside reference fixtures that also pin `generateBugsModel` to the editor's exact output.
