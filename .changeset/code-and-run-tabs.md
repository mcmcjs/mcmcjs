---
"doodleppl": minor
---

Replace the Script tab with Code and Run: the model code is rendered by one component shared with the floating panel, the Run tab offers a Jupyter notebook with an Open in Colab link, and every artifact is derived from the graph instead of generated on a button press and cached.
