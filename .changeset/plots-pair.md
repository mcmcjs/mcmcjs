---
"@mcmcjs/plots": minor
---

Add the pair (joint) plot: `pairData` pools two variables' draws with chain and divergence labels; `renderPairTerminal` draws a chain-colored scatter and `renderPairSVG` draws scatter points with divergent transitions highlighted in red (thinning dense runs while keeping every divergence).
