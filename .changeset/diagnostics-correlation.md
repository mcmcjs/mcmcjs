---
"@mcmcjs/diagnostics": minor
---

Add `pearson` and `spearman` correlation: `pearson(xs, ys)` is the product-moment coefficient (0 when fewer than two pairs or the denominator vanishes) and `spearman(xs, ys)` applies it to 1-based ranks (no tie-averaging).
