---
"@mcmcjs/doodlebugs": minor
---

Port the BUGS distribution catalog (26 distributions with parameter metadata), the built-in function set, and the graph validator (`validateGraph`) from the editor, so validation and authoring metadata live in the shared core.
