---
"@mcmcjs/plots": minor
---

Add the ecdf, cumulative-mean, and running-rhat plots (terminal, SVG, and interactive HTML). `ecdfData`, `cumulativeMeanData`, and `runningRhatData` build the data; running-rhat reuses the basic split-R-hat from `@mcmcjs/diagnostics` over an increasing prefix of draws.
