---
"doodleppl": minor
---

Bundle the example models into the package: loading an example no longer fetches from the JuliaBUGS repository, so examples work offline and on any host page, and every example is available in the embedded widget.
