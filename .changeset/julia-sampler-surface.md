---
"@mcmcjs/julia": minor
---

The driver runs HMC, HMCDA, and MH with per-algorithm options, selects the AD backend from the spec (ForwardDiff, ReverseDiff, or Mooncake; ReverseDiff joins the managed package set), thins draws, and seeds chains from named initial values.
