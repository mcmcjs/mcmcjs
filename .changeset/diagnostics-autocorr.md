---
"@mcmcjs/diagnostics": minor
---

Export `autocorr(chain, maxLag?)`, the normalized autocorrelation of a chain (lag 0 = 1) computed from the same FFT used for ESS, so consumers get one canonical estimator.
