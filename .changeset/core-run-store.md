---
"@mcmcjs/core": minor
---

Add the project-local run store: store discovery and layout under a hidden self-gitignored .mcmc directory, an append-only run ledger with cached convergence summaries, content-based run keys, and run refs (latest, @N, id prefix).
