---
"@mcmcjs/plots": minor
---

The violin, chain-intervals, pair, splom, and parallel-coordinates builders accept `chainIds`, keying chain labels and colors to chain identity rather than array position, so a plot over a chain subset keeps each chain's original number and color.
