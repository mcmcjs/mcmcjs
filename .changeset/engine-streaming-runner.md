---
"@mcmcjs/engine": minor
---

Add createStreamingRunner: a CommandRunner that streams a long subprocess's stdout/stderr live to this process's stderr (for install/precompile logs) instead of buffering them.
