---
"mcmcjs": minor
---

Add a `summary` command: a focused posterior stats table (mean, std, mcse, ess_bulk, ess_tail, r_hat, hdi) with `--json`, `--stdin`, `--warmup`, `--store`, and `--var` filtering.
