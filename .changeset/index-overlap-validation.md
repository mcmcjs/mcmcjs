---
"@mcmcjs/doodleppl": minor
---

Validate index-range overlap between nodes that share a variable name, mirroring the BUGS rule that several statements may define one variable only over disjoint ranges; seeded recursions such as a pinned first element beside a plate over 2:N stay valid.
