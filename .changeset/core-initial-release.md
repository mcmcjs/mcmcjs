---
"@mcmcjs/core": minor
---

Initial release: the `Samples` data model, parsers for MCMCChains JSON and ArviZ InferenceData JSON, format auto-detection via `parseSamples`, and a round-trip serializer.
