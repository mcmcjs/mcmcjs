---
"mcmcjs": minor
---

Add mcmc browse, an interactive browser over the project's runs and model files: filter as you type, then read a run's summary, diagnostics, per-variable traces, and plots, launch or repeat a fit, or delete a run; bare `mcmc` opens it when a terminal is attached and still prints the help otherwise.
