---
"@mcmcjs/plots": minor
---

Add violin, chain-intervals, and chain-intervals-all plots (terminal, SVG, and HTML). `violinData` reuses the density bandwidth for a peak-normalized KDE band per chain; `chainIntervalsData` and `chainIntervalsAllData` build per-chain and pooled q5/q25/q50/q75/q95 interval rows.
