---
"mcmcjs": minor
---

`mcmc run` gains `--algorithm` (NUTS, HMC, HMCDA, MH, Prior), --thin, and --adtype; mcmc export materializes the cached loglik.json; and mcmc plot gains the loo-pit calibration check via --loglik.
