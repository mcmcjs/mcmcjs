---
"@mcmcjs/doodleppl": minor
---

Export `discreteLatentNames` and `DISCRETE_DISTRIBUTIONS` from the package root and from `./core`, so a caller can ask which of a graph's variables are discrete latents without going through the Stan generator
