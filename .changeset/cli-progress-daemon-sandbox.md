---
"mcmcjs": minor
---

Show live per-chain sampling progress during run and fit, add --daemon (persistent Julia worker, 10x faster warm refits) with a daemon status/stop command group, and add mcmc sandbox: a throwaway shell seeded with a working example that is deleted on exit unless you choose to keep it.
