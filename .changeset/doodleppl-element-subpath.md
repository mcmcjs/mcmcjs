---
"doodleppl": minor
---

Expose the editor element on the `doodleppl/element` subpath: importing it registers `<doodle-ppl>` and exports the `DoodlePPLElement` class, so a host can mount the element declaratively or register an alias tag by subclassing.
