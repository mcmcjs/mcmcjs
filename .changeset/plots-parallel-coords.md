---
"@mcmcjs/plots": minor
---

Add the parallel-coordinates plot: `parallelCoordsData` builds per-variable axis bounds and one capped, subsampled polyline per draw; `renderParallelCoordsSVG` draws the axes and chain-colored polylines and `renderParallelCoordsTerminal` prints a per-variable min/mean/max summary.
