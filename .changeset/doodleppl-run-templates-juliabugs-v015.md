---
"@mcmcjs/doodleppl": minor
---

Author the run scripts as `.tpl` templates (inlined at build time) and modernize the JuliaBUGS run script to the v0.15 API: a callable `@bugs` model definition with an `AutoMooncake` AD backend, `initialize!` for inits, and FlexiChains output, replacing the removed `LogDensityProblemsAD`/`ADgradient`/`getparams` pattern.
