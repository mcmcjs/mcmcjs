---
"@mcmcjs/doodlebugs": minor
---

Initial release: parse DoodleBUGS graphs and generate BUGS model code (graph data model, topological sort, and the graph-to-`model { ... }` codegen ported from the editor), so the codegen has one shared home.
