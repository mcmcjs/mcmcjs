---
"mcmcjs": minor
---

Add `mcmc convert <graph.json>`: turn a DoodleBUGS graph into an idiomatic JuliaBUGS model file and a fit-able spec (backend `juliabugs` with the extracted `[data]`), so a graph can go straight to `mcmc fit`.
