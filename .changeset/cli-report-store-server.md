---
"mcmcjs": minor
---

`mcmc report` now serves the whole run store over the loopback handoff (the linked run, the ledger, and any run by id), and a new `--watch` flag keeps the server up so the report app can browse every run without file pickers.
