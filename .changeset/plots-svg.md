---
"@mcmcjs/plots": minor
---

Add SVG renderers for trace, density, histogram, autocorrelation, and forest plots (`renderTraceSVG`, etc.), producing standalone publication-quality vector figures from the same plot data the terminal renderers use. Re-exports `stackSvg` for composing multiple figures.
