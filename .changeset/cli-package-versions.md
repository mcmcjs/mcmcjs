---
"mcmcjs": minor
---

Pin and compare package versions: `[backend.packages]` in a spec, `--package name=version` on run (repeatable, flags win), and `mcmc fit --package-versions Turing=0.44,0.45` to run a spec across versions of a managed package, each in its own environment.
