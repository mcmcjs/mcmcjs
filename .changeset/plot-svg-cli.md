---
"mcmcjs": minor
---

`mcmc plot` gains `--format svg` for publication-quality export: it renders trace/density/histogram/autocorr/forest as SVG and, with multiple variables, stacks them into one document. Use `-o file.svg` to save (headless/CI friendly).
