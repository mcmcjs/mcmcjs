---
"mcmcjs": minor
---

`mcmc run` takes `--monitor <name>` to store only the named deterministic quantities alongside the parameters, and `--timeout <minutes>` to give a long fit more than the default 30 minutes.
