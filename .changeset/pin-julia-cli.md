---
"mcmcjs": patch
---

Default runs now target the pinned Julia version for reproducibility, and provisioning instantiates the shipped, resolved package set. Pass `--julia-version` to run on another channel.
