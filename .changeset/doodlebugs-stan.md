---
"@mcmcjs/doodlebugs": minor
---

Add the Stan generator as a `./stan` subpath export (model code, data and inits JSON, and a CmdStanPy run script), ported from the editor and verified byte-for-byte against its output on the twelve bundled example graphs.
