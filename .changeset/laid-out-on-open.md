---
"doodleppl": minor
---

An imported document that asks for a layout (`autoLayout`), or that carries no node positions, is laid out once when its canvas first renders; validation applies the variable-name rules of the language the document was imported from.
