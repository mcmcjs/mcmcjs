---
"@mcmcjs/julia": minor
---

JuliaBUGS posterior prediction in the driver: targets are blanked to latent nodes, the model is conditioned on the posterior parameter columns, and the targets are ancestral-sampled once per posterior draw with the run seed.
