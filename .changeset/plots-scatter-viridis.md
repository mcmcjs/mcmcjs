---
"@mcmcjs/plots": minor
---

Pair plots support an optional color-by-variable: `pairData(samples, x, y, { colorVar })` attaches a per-point color channel and `renderPairSVG` shades points through the viridis colormap with a gradient legend (color-by-chain remains the default).
