---
"mcmcjs": minor
---

Stan support across the CLI: `mcmc run model.stan` fits through a local CmdStan with the full store/diagnose workflow, `mcmc fit` accepts Stan specs, `mcmc setup --engine stan` provisions CmdStan, and `doctor`/`engines` report the Stan toolchain.
