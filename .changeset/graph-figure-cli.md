---
"mcmcjs": minor
---

Add `mcmc figure <graph>` to draw a DoodleBUGS graph as a black-and-white figure for papers, as TikZ for LaTeX (with `--standalone` for a document pdflatex compiles on its own) or as SVG.
