---
"@mcmcjs/doodleppl": minor
---

`validateGraph` takes a `language` option and applies that language's variable-name rules (Stan names use underscores, BUGS names use dots); a graph document may now carry `language` and `autoLayout`, which the parsers set.
