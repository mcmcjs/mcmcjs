---
"@mcmcjs/core": minor
---

RunBundle: a self-contained single-file run format (ledger entry, resolved spec, model source, samples) with parseRunBundle validation, shared by the CLI exporter and the report web app.
