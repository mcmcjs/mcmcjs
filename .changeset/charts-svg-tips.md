---
"@mcmcjs/charts": minor
---

Hover and theming primitives for SVG plots: attachSvgTips (a delegated tooltip runtime matching the uPlot cursor tooltip) and SVG_TIPS_CSS in the dom entry, data-tip support across the SVG mark helpers, and frame text drawn through --mcmc theme variables with light fallbacks.
