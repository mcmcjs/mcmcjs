---
"@mcmcjs/julia": minor
---

Build a JuliaBUGS Metropolis block from AdvancedMH, which replaced the removed `IndependentMH` in 0.17: an adaptive random walk on a continuous block, a lattice walk on unbounded counts, and JuliaBUGS's own exact draw on a finite discrete one
