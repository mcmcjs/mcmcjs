---
"mcmcjs": minor
---

`mcmc run` takes a program in BUGS syntax as a `.bugs` file, or a BUGS example folder as the JuliaBUGS repository keeps them, picking up its `data.json`, published initial values, and published quantities; `--inits <file>` supplies starting values for any model.
