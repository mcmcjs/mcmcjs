---
"@mcmcjs/charts": minor
---

`modebarButtons` on the interaction options appends custom buttons (icon, title, onClick) to the hover modebar, so an embedding app can add its own actions such as a configurable export or a fullscreen view.
