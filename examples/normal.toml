schema_version = "0"
seed = 42

[backend]
id = "juliabugs"
runtime = "julia"
version = "release"

[model]
kind = "file"
path = "./normal.jl"
entry = "build_model"

[sampler]
algorithm = "NUTS"
draws = 1000
warmup = 1000
chains = 4
adapt_delta = 0.8

[data]
N = 5
y = [1.2, 0.8, 1.5, 1.1, 0.9]
